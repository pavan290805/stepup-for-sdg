import React from 'react';
import ColorAccentLine from './ColorAccentLine';

const MultiColorBar = ({ className = '' }) => {
  return <ColorAccentLine className={className} />;
};

export default MultiColorBar;
